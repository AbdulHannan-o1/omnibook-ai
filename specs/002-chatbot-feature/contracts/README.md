# API Contracts for Chatbot Feature

This directory will contain API contracts (e.g., OpenAPI/Swagger specifications) if a dedicated HTTP endpoint for the chatbot backend is implemented. For the current phase, direct integration with the OpenAI Agent SDK is envisioned, with UI interactions handled by existing application mechanisms.
